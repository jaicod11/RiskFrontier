# --- Web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database --------------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Analytics (used by the risk / optimisation / backtest services) -------
pandas==2.2.3
numpy==2.2.1
scipy==1.15.0
# 1.x uses curl_cffi browser impersonation; 0.2.x is blocked by Yahoo
# with HTTP 429 and returns empty frames for every ticker.
yfinance==1.7.0

# --- Testing ---------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1
